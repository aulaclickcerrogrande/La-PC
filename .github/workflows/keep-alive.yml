name: Supabase Keep-Alive

on:
  schedule:
    # Se ejecuta todos los días a las 12:00 UTC
    - cron: '0 12 * * *'
  workflow_dispatch: # Permite ejecutarlo manualmente desde GitHub para probar

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase API
        run: |
          curl -i -X GET "https://tarcumxnttixqliefpbv.supabase.co/rest/v1/components?select=id&limit=1" \
          -H "apikey: sb_publishable_2Vux9qDq4vK6PVMe7PFcPQ_cGneiDt0" \
          -H "Authorization: Bearer sb_publishable_2Vux9qDq4vK6PVMe7PFcPQ_cGneiDt0"
